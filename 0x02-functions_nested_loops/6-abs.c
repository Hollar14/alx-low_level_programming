#include "main.h"

/**
* _abs - print the asolute value of an int
* @n: The interger to be checked
*
* Return: the abs of the int
*/

int abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
